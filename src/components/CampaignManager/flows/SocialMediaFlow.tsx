import React from 'react';
import { Box, Text } from '@mantine/core';

const SocialMediaFlow: React.FC = () => {
  return (
    <Box>
      <Text fw={500} mb="md">Social Media Campaign</Text>
      <Text>Your post will be shared on the selected platforms. No additional configuration is needed.</Text>
    </Box>
  );
};

export default SocialMediaFlow;